NODE_ENV=test

# Test database. Unit tests stub this; integration tests need a live PG instance.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/synapse_test"
ENCRYPTION_KEY="dGVzdC1lbmNyeXB0aW9uLWtleS0zMi1ieXRlcy1sb25nMQ=="

# NOTE: auth paths may move to a subpath in a future release
# AUTH_BASE_URL=https://auth.local

# AUTH_BASE_URL="https://localhost:8000"
# CORS_ALLOWED_ORIGINS=https://localhost:3000,https://localhost:3001
# PORT=0

# NOTE: auth paths may move to a subpath in a future release
AUTH_BASE_URL="https://localhost:8000"

# Enable route protection for auth tests
PROTECT_ROUTES="true"

# Shared secret for gateway-to-API internal requests
GATEWAY_SECRET="test-gateway-secret"

# Shared secret for billing webhook signature verification
BILLING_WEBHOOK_SECRET="test-billing-secret"
